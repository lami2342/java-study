# 2023년 3회 정보처리기사 실기 복원문제

---

## 1번 - Java 다형성

다음 Java 코드의 실행 결과를 쓰시오.

A 클래스와 B 클래스가 있으며, B 클래스는 A 클래스를 상속받아 메서드를 오버라이딩한다. 다형성을 활용하여 객체를 생성하고 메서드를 호출할 때의 출력 결과를 작성하시오.

---

## 2번 - 개방형 표준 프로토콜

다음 설명에 해당하는 용어를 쓰시오.

인터넷 사용자가 비밀번호를 제공하지 않고 다른 웹사이트 상의 자신의 정보에 대해 접근 권한을 부여할 수 있는 개방형 표준 프로토콜이다. 6가지 특징이 제시되며, 제3자 애플리케이션에 사용자의 아이디와 패스워드를 직접 제공하지 않고도 인증 및 권한 부여를 할 수 있다.

---

## 3번 - Linux 파일 권한

Linux 환경에서 파일에 다음과 같은 권한을 부여하려고 한다. 빈칸을 채우시오.

- 사용자(User): 읽기, 쓰기, 실행
- 그룹(Group): 읽기, 실행
- 기타(Other): 실행

```
(1) (2) a.txt
```

(1) 사용할 명령어를 쓰시오.
(2) 권한을 숫자(8진수)로 표현하시오.

---

## 4번 - C언어 완전수

다음 C 프로그램의 실행 결과를 쓰시오.

1부터 1000 사이의 완전수(Perfect Number)를 찾아 그 합을 출력하는 프로그램이다. 완전수란 자기 자신을 제외한 약수의 합이 자기 자신과 같은 수를 말한다.

---

## 5번 - C언어 구조체 멤버 접근

C언어에서 포인터를 통해 구조체의 멤버에 접근할 때 사용하는 연산자 기호를 쓰시오.

---

## 6번 - SQL UNION

다음 SQL 쿼리의 실행 결과를 쓰시오.

두 개의 SELECT 문을 UNION으로 결합하고 내림차순(DESC)으로 정렬하는 쿼리가 주어진다.

---

## 7번 - 접근 통제

다음 설명에 해당하는 접근 통제 방식을 각각 쓰시오.

(1) 중앙에서 관리하며, 보안 등급(레이블)을 기반으로 접근 권한을 부여하는 방식
(2) 사용자의 역할(Role)에 기반하여 권한을 부여하는 방식
(3) 데이터 소유자가 자유롭게 다른 사용자에게 접근 권한을 부여할 수 있는 방식

---

## 8번 - C언어 재귀함수

다음 C 프로그램에서 재귀함수를 이용하여 f(7)을 호출했을 때의 실행 결과를 쓰시오.

팩토리얼(Factorial)을 계산하는 재귀함수가 주어진다.

---

## 9번 - 자료 전송 방식

다음 설명에 해당하는 용어를 쓰시오.

고정 길이의 53바이트 셀(Cell)을 기반으로 데이터를 전송하는 비동기 전송 방식이다. 음성, 영상, 데이터 등 다양한 서비스를 하나의 네트워크에서 처리할 수 있다.

---

## 10번 - C언어 포인터 문자열

다음 C 프로그램의 실행 결과를 쓰시오.

```c
#include <stdio.h>
int main(){
    char *p = "KOREA";
    printf("%s\n", p);
    printf("%s\n", p+1);
    printf("%c\n", *p);
    printf("%c\n", *(p+3));
    printf("%c\n", *(p+2));
}
```

---

## 11번 - Java 상속과 다형성

Java에서 Parent 클래스와 Child 클래스가 주어진다. 상속과 다형성을 활용한 코드의 실행 결과를 쓰시오.

---

## 12번 - 네트워크 기술

다음 설명에 해당하는 용어를 쓰시오.

사설 IP 주소를 공인 IP 주소로 변환하거나, 반대로 공인 IP 주소를 사설 IP 주소로 변환하는 네트워크 기술이다. 내부 네트워크에서 사용하는 사설 IP 주소와 외부 인터넷에서 사용하는 공인 IP 주소 간의 변환을 수행한다.

---

## 13번 - Java 에러 위치

다음 Java 코드에서 에러가 발생하는 라인 번호를 쓰시오.

static 메서드에서 인스턴스 변수에 직접 접근하려고 하는 코드가 주어진다.

---

## 14번 - Python 입출력

Python에서 입력받은 문자열을 특정 구분자를 기준으로 분리할 때 사용하는 메서드 이름을 쓰시오.

---

## 15번 - UML 다이어그램

다음 설명에 해당하는 UML 다이어그램의 이름을 쓰시오.

폴더 모양의 기호를 사용하며, 시스템의 구성 요소 간의 관계를 계층적으로 표현하는 구조(Structure) 다이어그램이다. 모델 요소들을 그룹화하여 관리하는 데 사용된다.

---

## 16번 - 소프트웨어 테스트

다음 설명에 해당하는 소프트웨어 테스트 기법의 이름을 쓰시오.

입력값을 유사한 특성에 따라 여러 그룹(클래스)으로 분류하고, 각 그룹에서 대표값을 선택하여 테스트하는 블랙박스 테스트 기법이다.

---

## 17번 - 클라우드 서비스

다음 설명에 해당하는 클라우드 서비스 모델을 각각 쓰시오.

(1) 서버, 스토리지, 네트워크 등 IT 인프라를 가상화하여 제공하는 서비스
(2) 애플리케이션 개발 및 배포를 위한 플랫폼을 제공하는 서비스
(3) 소프트웨어를 인터넷을 통해 서비스 형태로 제공하는 서비스

---

## 18번 - 라우팅 프로토콜

다음 설명에 해당하는 라우팅 프로토콜의 이름을 쓰시오.

홉 카운트(Hop Count)를 기반으로 경로를 결정하는 거리 벡터(Distance Vector) 라우팅 프로토콜이다. 최대 홉 수는 15이며, 16 이상은 도달 불가능으로 간주한다.

---

## 19번 - 관계 대수 기호

다음 관계 대수 연산에 해당하는 기호를 각각 쓰시오.

(1) 조인(Join)
(2) 프로젝트(Project)
(3) 셀렉트(Select)
(4) 디비전(Division)

---

## 20번 - 데이터베이스 무결성

다음 설명에 해당하는 무결성의 종류를 쓰시오.

외래키(Foreign Key)를 통해 관련된 테이블 간의 데이터 일관성을 유지하는 것을 말한다. 외래키 값은 참조하는 테이블의 기본키 값이거나 NULL이어야 한다.
