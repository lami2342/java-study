# 2024년 3회 정보처리기사 실기 복원문제

---

## 1번 - Java 코드 출력값

다음 Java 코드의 출력값을 쓰시오.

```java
public class Main{
  static String[] s = new String[3];
  static void func(String[]s, int size){
    for(int i=1; i<size; i++){
      if(s[i-1].equals(s[i])){
        System.out.print("O");
      }else{
        System.out.print("N");
      }
    }
    for (String m : s){
      System.out.print(m);
    }
  }
  public static void main(String[] args){
    s[0] = "A";
    s[1] = "A";
    s[2] = new String("A");
    func(s, 3);
  }
}
```

---

## 2번 - Python 코드 출력값

다음 Python 코드의 출력값을 쓰시오.

```python
def func(lst):
  for i in range(len(lst) //2):
    lst[i], lst[-i-1] = lst[-i-1], lst[i]
lst = [1,2,3,4,5,6]
func(lst)
print(sum(lst[::2]) - sum(lst[1::2]))
```

---

## 3번 - SQL 쿼리 결과값

다음 SQL 쿼리의 실행 결과값을 쓰시오.

```sql
SELECT count(*) FROM employee AS e JOIN project AS p ON e.project_id = p.project_id
WHERE p.name IN (
    SELECT name FROM project p WHERE p.project_id IN (
        SELECT project_id FROM employee GROUP BY project_id HAVING count(*) < 2
    )
);
```

---

## 4번 - LRU 페이지 부재 횟수

LRU 페이지 교체 알고리즘을 사용하고, 프레임이 3개일 때 다음 페이지 참조 순서에 대한 페이지 부재(Page Fault) 횟수를 구하시오.

**페이지 참조 순서:** 7 0 1 2 0 3 0 4 2 3 0 3 2 1 2 0 1 7 0 1

---

## 5번 - 네트워크 공격

IP/ICMP를 악용하여 다량의 ICMP Echo Reply를 이용한 공격 기법의 이름을 쓰시오.

---

## 6번 - GoF 디자인 패턴

클래스나 객체들이 서로 상호작용하는 방법을 정의하며, 알고리즘을 캡슐화하고, Chain of Responsibility, Command, Observer 등이 해당되는 GoF 디자인 패턴의 분류를 쓰시오.

---

## 7번 - C언어 코드 출력값

다음 C 코드의 출력값을 쓰시오.

```c
#include <stdio.h>
int func(){
  static int x = 0;
  x += 2;
  return x;
}
int main(){
  int x = 1;
  int sum = 0;
  for(int i=0; i<4; i++) {
    x++;
    sum += func();
  }
  printf("%d", sum);
  return 0;
}
```

---

## 8번 - 무결성 제약조건

기본 키(Primary Key)에 NULL 값이 들어가면 위반되는 무결성 제약조건의 이름을 쓰시오.

---

## 9번 - URL 구조

URL의 구성 요소인 scheme, authority, path, query, fragment의 순서를 올바르게 매칭하시오.

---

## 10번 - Python 코드 출력값

다음 Python 코드의 출력값을 쓰시오.

```python
def func(value):
    if type(value) == type(100):
        return 100
    elif type(value) == type(""):
        return len(value)
    else:
        return 20
a = '100.0'
b = 100.0
c = (100, 200)
print(func(a) + func(b) + func(c))
```

---

## 11번 - Java 코드 출력값

다음 Java 코드의 출력값을 쓰시오.

```java
public class Main{
  public static void main(String[] args){
    Base a = new Derivate();
    Derivate b = new Derivate();
    System.out.print(a.getX() + a.x + b.getX() + b.x);
  }
}
class Base{
  int x = 3;
  int getX(){ return x * 2; }
}
class Derivate extends Base{
  int x = 7;
  int getX(){ return x * 3; }
}
```

---

## 12번 - C언어 연결리스트 출력값

다음 C 코드의 출력값을 쓰시오.

```c
struct Node { int value; struct Node* next; };
void func(struct Node* node){
  while(node != NULL && node->next != NULL){
    int t = node->value;
    node->value = node->next->value;
    node->next->value = t;
    node = node->next->next;
  }
}
int main(){
  struct Node n1 = {1, NULL};
  struct Node n2 = {2, NULL};
  struct Node n3 = {3, NULL};
  n1.next = &n3;
  n3.next = &n2;
  func(&n1);
  // 순회 출력
}
```

---

## 13번 - 테스트 커버리지

다음 설명에 해당하는 테스트 커버리지의 이름을 각각 쓰시오.

1. 모든 문장을 최소 1번 실행하는 커버리지
2. 모든 분기를 최소 1번 실행하는 커버리지
3. 각 개별 조건이 참/거짓으로 평가되는 커버리지

---

## 14번 - UML 관계

다음 설명에 해당하는 UML 관계의 이름을 각각 쓰시오.

(1) 클래스 간의 관계를 나타내며 서로 연결된 관계
(2) 상위 클래스와 하위 클래스 간의 is-a 관계
(3) 한 클래스가 다른 클래스를 일시적으로 사용하는 관계

---

## 15번 - 데이터베이스 키

다음 설명에 해당하는 데이터베이스 키의 이름을 각각 쓰시오.

(1) 다른 테이블의 기본키를 참조하는 키
(2) 유일하게 식별하는 최소 속성 집합인 키
(3) 후보키 중 기본키를 제외한 키
(4) 유일하게 식별하는 속성 집합인 키

---

## 16번 - C언어 포인터 코드 출력값

다음 C 코드의 출력값을 쓰시오.

```c
#include <stdio.h>
void func(int** arr, int size){
  for(int i=0; i<size; i++){
    *(*arr + i) = (*(*arr + i) + i) % size;
  }
}
int main(){
  int arr[] = {3, 1, 4, 1, 5};
  int* p = arr;
  int** pp = &p;
  int num = 6;
  func(pp, 5);
  num = arr[2];
  printf("%d", num);
  return 0;
}
```

---

## 17번 - 네트워크 용어

공용 네트워크를 통해 사설 네트워크를 확장하고, IP 주소를 숨기며, IPsec, SSL, L2TP 등의 프로토콜을 사용하는 기술의 이름을 쓰시오.

---

## 18번 - Java 예외 처리 출력값

다음 Java 코드의 출력값을 쓰시오.

```java
public class ExceptionHandling {
  public static void main(String[] args) {
    int sum = 0;
    try {
      func();
    } catch (NullPointerException e) {
      sum = sum + 1;
    } catch (Exception e) {
      sum = sum + 10;
    } finally {
      sum = sum + 100;
    }
    System.out.print(sum);
  }
  static void func() throws Exception {
    throw new NullPointerException();
  }
}
```

---

## 19번 - Java 제네릭 코드 출력값

다음 Java 코드의 출력값을 쓰시오.

```java
class Main {
  public static class Collection<T>{
    T value;
    public Collection(T t){ value = t; }
    public void print(){ new Printer().print(value); }
    class Printer{
      void print(Integer a){ System.out.print("A" + a); }
      void print(Object a){ System.out.print("B" + a); }
      void print(Number a){ System.out.print("C" + a); }
    }
  }
  public static void main(String[] args) {
    new Collection<>(0).print();
  }
}
```

---

## 20번 - 네트워크 용어

중앙 관리 없이 임시로 구성되며, 무선 통신으로 직접 연결하고, 긴급 상황에 활용되는 네트워크의 이름을 쓰시오.
