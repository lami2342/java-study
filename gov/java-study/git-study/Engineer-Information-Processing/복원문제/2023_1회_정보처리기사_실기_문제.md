# 2023년 1회 정보처리기사 실기 복원문제

---

## 1번 - Java 출력값

다음 Java 코드의 실행 결과를 쓰시오.

```java
class Static{
    public int a = 20;
    static int b = 0;
}
public class Main{
    public static void main(String[] args) {
        int a;
        a = 10;
        Static.b = a;
        Static st = new Static();
        System.out.println(Static.b++);
        System.out.println(st.b);
        System.out.println(a);
        System.out.print(st.a);
    }
}
```

---

## 2번 - C언어 출력값

다음 C 코드의 실행 결과를 쓰시오.

```c
#include <stdio.h>
int main(){
    char a[] = "Art";
    char* p = NULL;
    p = a;
    printf("%s\n", a);
    printf("%c\n", *p);
    printf("%c\n", *a);
    printf("%s\n", p);
    for(int i = 0; a[i] != '\0'; i++)
        printf("%c", a[i]);
}
```

---

## 3번 - C언어 출력값

다음 C 코드의 실행 결과를 쓰시오.

```c
#include <stdio.h>
int main(){
    char* a = "qwer";
    char* b = "qwtety";
    for(int i = 0; a[i] != '\0'; i++){
        for(int j = 0; b[j] != '\0'; j++){
            if(a[i] == b[j]) printf("%c", a[i]);
        }
    }
}
```

---

## 4번 - 웹 기술 용어

다음이 설명하는 기술 용어를 쓰시오.

> JavaScript와 XML을 이용한 비동기적 정보 교환 기법

---

## 5번 - 네트워크 통신 방식

다음 설명에 해당하는 용어를 각각 쓰시오.

(1) 회선교환과 데이터그램의 장점을 결합하였으며, ATM 기술에 사용된다.

(2) 패킷교환 방식으로 IP 주소를 사용하며, 각 패킷이 독립적인 경로로 전송된다.

---

## 6번 - VPN 프로토콜

다음이 설명하는 프로토콜을 쓰시오.

> 계층 2 터널링 프로토콜로, L2F와 PPTP를 결합한 방식이며, ISP를 이용한다.

---

## 7번 - 원격 접속 프로토콜

다음이 설명하는 프로토콜을 쓰시오.

> 다른 컴퓨터에 로그인하여 보안 접속이 가능하고, IP 스푸핑을 방지하며, 포트 22번을 사용한다.

---

## 8번 - 악성코드 분류

다음 설명에 해당하는 악성코드 유형을 각각 쓰시오.

(1) 스스로 복제하며 독자적으로 실행되고, 파일 전송 기능을 악용한다.

(2) 정상 프로그램으로 위장하며 백도어를 생성하고, 자가복제가 불가능하다.

(3) 감염 대상이 필요하며 자기 복제 기능이 있는 감염 형태의 악성코드이다.

---

## 9번 - C언어 빈칸 채우기

다음은 이진수를 십진수로 변환하는 C 코드이다. 빈칸 (a)와 (b)에 들어갈 내용을 쓰시오.

```c
#include <stdio.h>
int main() {
    int input = 101110;
    int di = 1;
    int sum = 0;
    while (1) {
        if (input == 0) break;
        else {
            sum = sum + (input (a)(b)) * di;
            di = di * 2;
            input = input / 10;
        }
    }
    printf("%d", sum);
    return 0;
}
```

---

## 10번 - 네트워크 공격

다음이 설명하는 공격 기법을 쓰시오.

> TCP/IP의 IP 패킷 처리 알림 프로토콜을 이용하며, ping 명령어를 연속적으로 전송하여 공격하는 방식이다.

---

## 11번 - 디자인 패턴

다음이 설명하는 디자인 패턴의 이름을 쓰시오.

> 다른 객체에 대한 접근을 제어하기 위해 대리 역할을 하며, 실제 객체 호출 시 행위를 가로채는 패턴이다.

---

## 12번 - 데이터베이스 용어

다음 설명에 해당하는 데이터베이스 용어를 각각 쓰시오.

(1) 순서가 있는 열거이며, 중복을 허용하고, 여러 객체를 포함한다.

(2) 특정 시점의 릴레이션 내용이며, 저장된 데이터 전체를 의미한다.

(3) 유니크한 값의 개수를 나타내며, 중복 수치의 지표이다.

---

## 13번 - SQL 작성

학생 테이블에서 이름이 '민수'인 튜플을 삭제하는 SQL문을 작성하시오.

---

## 14번 - Java 빈칸 채우기

다음 Java 정렬 코드의 빈칸 (1)과 (2)에 들어갈 내용을 쓰시오.

```java
public class Sort {
    public static void swap(int[] arr, int idx1, int idx2){
        int temp = arr[idx1];
        arr[idx1] = arr[idx2];
        arr[(1)] = temp;
    }
    // Usort 메서드에서 재귀호출 Usort(item, (2))
}
```

---

## 15번 - Python 출력값

다음 Python 코드의 실행 결과를 쓰시오.

```python
a = {'한국', '중국', '일본'}
a.add('베트남')
a.add('중국')
a.remove('일본')
a.update({'홍콩', '한국', '태국'})
print(a)
```

---

## 16번 - SQL 작성

성적 테이블에서 과목별 평균 점수가 90점 이상인 과목의 과목이름, 최소점수, 최대점수를 검색하는 SQL문을 작성하시오.

---

## 17번 - Java 출력값

다음 Java 코드의 실행 결과를 쓰시오.

```java
abstract class Vehicle {
    String name;
    abstract public String getName(String val);
    public String getName() {
        return "Vehicle name: " + name;
    }
}
class Car extends Vehicle {
    public Car(String val) {
        name = super.name = val;
    }
    public String getName(String val) {
        return "Car name:" + val;
    }
    public String getName(byte val[]) {
        return "Car name:" + val;
    }
}
public class Main {
    public static void main(String[] args) {
        Vehicle obj = new Car("Spark");
        System.out.println(obj.getName());
    }
}
```

---

## 18번 - 데이터베이스 스키마

다음 설명에 해당하는 스키마의 종류를 각각 쓰시오.

(1) 사용자 입장에서의 논리적 구조이며, 서브스키마라고도 한다.

(2) 전체 데이터베이스의 논리적 구조이며, 하나만 존재한다.

(3) 물리적 저장 방법을 정의하며, 시스템 프로그래머가 관리한다.

---

## 19번 - 테스트 커버리지

제어 흐름 그래프에서 분기 커버리지(Branch Coverage)를 만족하는 경로를 쓰시오.

---

## 20번 - Java 출력값

다음 Java 코드의 실행 결과를 쓰시오.

```java
class Parent {
    int x = 100;
    Parent() { this(500); }
    Parent(int x) { this.x = x; }
    int getX() { return x; }
}
class Child extends Parent {
    int x = 4000;
    Child() { this(5000); }
    Child(int x) { this.x = x; }
}
public class Main {
    public static void main(String[] args) {
        Child obj = new Child();
        System.out.println(obj.getX());
    }
}
```
