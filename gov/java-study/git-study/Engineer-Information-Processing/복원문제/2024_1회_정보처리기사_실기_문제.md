# 2024년 1회 정보처리기사 실기 복원문제

---

## 1번 - Java 프로그래밍

다음 Java 코드의 실행 결과를 쓰시오.

```java
class Connection {
    private static Connection _inst = null;
    private int count = 0;
    static public Connection get() {
        if(_inst == null) {
            _inst = new Connection();
            return _inst;
        }
        return _inst;
    }
    public void count() { count++; }
    public int getCount() { return count; }
}
public class main {
    public static void main(String[] args) {
        Connection conn1 = Connection.get();
        conn1.count();
        Connection conn2 = Connection.get();
        conn2.count();
        Connection conn3 = Connection.get();
        conn3.count();
        conn1.count();
        System.out.print(conn1.getCount());
    }
}
```

---

## 2번 - C언어 프로그래밍

다음 C 코드의 실행 결과를 쓰시오.

```c
#include <stdio.h>
int main() {
    int v1 = 0, v2 = 35, v3 = 29;
    if(v1 > v2 ? v2 : v1) {
        v2 = v2 << 2;
    }else{
        v3 = v3 << 2;
    }
    printf("%d", v2+v3);
}
```

---

## 3번 - 소프트웨어 공학 (응집도)

다음 보기의 응집도를 높은 순서대로 나열하시오.

> ㄱ. 기능 응집도
> ㄴ. 교환 응집도
> ㄷ. 우연 응집도
> ㄹ. 시간 응집도

---

## 4번 - C언어 프로그래밍

다음 C 코드의 실행 결과를 쓰시오.

```c
#include <stdio.h>
#include <string.h>
void reverse(char* str){
    int len = strlen(str);
    char temp;
    char*p1 = str;
    char*p2 = str + len - 1;
    while(p1<p2){
        temp = *p1;
        *p1 = *p2;
        *p2 = temp;
        p1++;
        p2--;
    }
}
int main(int argc, char* argv[]){
    char str[100] = "ABCDEFGH";
    reverse(str);
    int len = strlen(str);
    for(int i=1; i<len; i+=2){
        printf("%c",str[i]);
    }
    printf("\n");
    return 0;
}
```

---

## 5번 - 네트워크 (IP 주소)

라우터를 통한 할당 가능한 IP 주소를 모두 고르시오.

---

## 6번 - 데이터베이스 (정규화)

주어진 릴레이션이 만족하는 정규형을 판단하시오.

---

## 7번 - 네트워크 (라우팅 프로토콜)

다음 설명에 해당하는 라우팅 프로토콜을 쓰시오.

> - 링크 상태(Link State) 라우팅 프로토콜이다.
> - 내부 게이트웨이 프로토콜(IGP)이다.
> - Dijkstra 알고리즘을 사용한다.

---

## 8번 - 데이터베이스 (조인)

다음 설명에 해당하는 조인의 종류를 각각 쓰시오.

> (1) 두 릴레이션의 속성값을 비교하여 조건을 만족하는 튜플을 반환하는 조인
> (2) '=' 등호를 사용하여 속성값이 일치하는 튜플을 반환하는 조인
> (3) 조인 결과에서 중복된 속성을 제거하는 조인

---

## 9번 - 운영체제 (페이지 교체 알고리즘)

프레임 수가 3개일 때, 다음 페이지 참조 순서에 대해 각 알고리즘의 페이지 부재(Page Fault) 횟수를 구하시오.

> 참조 순서: 1, 2, 3, 1, 2, 4, 1, 2, 5, 7

> (1) LRU
> (2) LFU

---

## 10번 - Java 프로그래밍

다음 Java 코드의 실행 순서를 쓰시오.

---

## 11번 - C언어 프로그래밍

다음 C 코드의 실행 결과를 쓰시오.

```c
#include <stdio.h>
typedef struct{
    int accNum;
    double bal;
}BankAcc;

double sim_pow(double base, int year){
    int i;
    double r = 1.0;
    for(i=0; i<year; i++){
        r = r*base;
    }
    return r;
}

void initAcc(BankAcc *acc, int x, double y){
    acc->accNum = x;
    acc->bal = y;
}

void xxx(BankAcc *acc, double *en){
    if (*en > 0 && *en < acc->bal) {
        acc->bal = acc->bal - *en;
    }else{
        acc->bal = acc->bal + *en;
    }
}

void yyy(BankAcc *acc){
    acc->bal = acc->bal * sim_pow((1+0.1), 3);
}

int main(){
    BankAcc myAcc;
    initAcc(&myAcc, 9981, 2200.0);
    double amount = 100.0;
    xxx(&myAcc, &amount);
    yyy(&myAcc);
    printf("%d and %.2f", myAcc.accNum, myAcc.bal);
    return 0;
}
```

---

## 12번 - Python 프로그래밍

다음 Python 코드의 실행 결과를 쓰시오.

```python
a = ["Seoul", "Kyeonggi", "Incheon", "Daejun", "Daegu", "Pusan"]
str = "S"
for i in a:
    str = str + i[1]
print(str)
```

---

## 13번 - 데이터베이스 (SQL)

다음 SQL 쿼리의 실행 결과를 쓰시오.

```sql
SELECT B FROM R1 WHERE C IN (SELECT C FROM R2 WHERE D='k');
```

---

## 14번 - 소프트웨어 테스트 (커버리지)

각 개별 조건식이 다른 개별 조건식에 영향을 받지 않고 전체 조건식의 결과에 독립적으로 영향을 미치는 중요 조합을 중심으로 테스트하는 커버리지의 이름을 쓰시오.

---

## 15번 - 정보보안 (악성 프로그램)

다음 설명에 해당하는 악성 프로그램의 이름을 쓰시오.

> - 시스템 콜을 해킹하여 동작한다.
> - 운영체제 커널 영역에서 작동한다.
> - 안티바이러스 소프트웨어를 우회할 수 있다.

---

## 16번 - Java 프로그래밍

다음 Java 코드의 실행 결과를 쓰시오.

```java
class classOne {
    int a, b;
    public classOne(int a, int b) { this.a = a; this.b = b; }
    public void print() { System.out.println(a*b); }
}
class classTwo extends classOne {
    int po = 3;
    public classTwo(int i) { super(i, i+1); }
    public void print() { System.out.println(po*po); }
}
public class main {
    public static void main(String[] args) {
        classOne one = new classTwo(10);
        one.print();
    }
}
```

---

## 17번 - 정보보안 (보안 공격)

다음 설명에 해당하는 보안 공격의 이름을 쓰시오.

> - 명확한 표적을 대상으로 한다.
> - 다양한 기술을 사용한다.
> - 침투 → 탐색 → 수집 → 유출의 4단계로 진행된다.

---

## 18번 - 데이터베이스 (SQL)

다음 SQL 쿼리의 실행 결과를 쓰시오.

```sql
SELECT COUNT(*) FROM TABLE WHERE EMPNO > 100 AND SAL >= 3000 OR EMPNO = 200;
```

---

## 19번 - C언어 프로그래밍

다음 C 코드의 실행 결과를 쓰시오.

```c
#include<stdio.h>
#include<ctype.h>
int main(){
    char*p = "It is 8";
    char result[100];
    int i;
    for(i=0; p[i]!='\0'; i++){
        if(isupper(p[i]))
            result[i] = (p[i]-'A'+5)%25 + 'A';
        else if(islower(p[i]))
            result[i] = (p[i]-'a'+10)%26 + 'a';
        else if(isdigit(p[i]))
            result[i] = (p[i]-'0'+3)%10 + '0';
        else
            result[i] = p[i];
    }
    result[i] = '\0';
    printf("%s\n",result);
    return 0;
}
```

---

## 20번 - 소프트웨어 공학 (디자인 패턴)

다음 설명에 해당하는 디자인 패턴의 이름을 쓰시오.

> - 서로 연관되거나 의존적인 객체들의 조합을 만드는 인터페이스를 제공한다.
> - Kit라고도 불린다.
