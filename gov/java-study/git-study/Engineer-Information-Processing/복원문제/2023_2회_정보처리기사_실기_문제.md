# 2023년 2회 정보처리기사 실기 복원문제

---

## 1번 - C언어 빈칸 채우기

입력값 54321을 입력했을 때 출력이 43215가 되도록 빈칸을 채우시오.

```c
int main(void) {
    int n[5];
    int i;
    for (i = 0; i < 5; i++) {
        printf("숫자를 입력해주세요 : ");
        scanf("%d", &n[i]);
    }
    for (i = 0; i < 5; i++) {
        printf("%d", (            ) );
    }
    return 0;
}
```

---

## 2번 - Java 화폐 계산

4620원을 1000원, 500원, 100원, 10원 단위로 나누어 각 화폐의 개수를 구하는 프로그램에서 빈칸을 채우시오.

---

## 3번 - C언어 포인터/문자열

다음 코드에서 "홍길동", "김철수", "박영희"를 순서대로 입력했을 때 출력 결과를 쓰시오.

```c
#include<stdio.h>
#include<stdlib.h>
char n[30];
char *test() {
    printf("입력하세요 : ");
    gets(n);
    return n;
}
int main() {
    char * test1;
    char * test2;
    char * test3;
    test1 = test();
    test2 = test();
    test3 = test();
    printf("%s\n",test1);
    printf("%s\n",test2);
    printf("%s",test3);
}
```

---

## 4번 - SQL INSERT

학생 테이블에 다음 데이터를 삽입하는 SQL문을 작성하시오.

| 학번 | 이름 | 학년 | 전공 | 전화번호 |
|------|------|------|------|----------|
| 9830287 | 뉴진스 | 3 | 경영학개론 | 010-1234-1234 |

---

## 5번 - C언어 switch-case

다음 코드의 출력 결과를 쓰시오.

```c
#include <stdio.h>
void main(){
    int n[3] = {73, 95, 82};
    int sum = 0;
    for(int i=0;i<3;i++){
        sum += n[i];
    }
    switch(sum/30){
        case 10:
        case 9: printf("A");
        case 8: printf("B");
        case 7:
        case 6: printf("C");
        default: printf("D");
    }
}
```

---

## 6번 - 테스트 커버리지

각 조건식에 대해 참(True)과 거짓(False)이 모두 한 번 이상 수행되는 테스트 커버리지의 이름을 쓰시오.

---

## 7번 - C언어 4의 배수

1부터 2023까지의 정수 중 4의 배수의 개수를 구하시오.

---

## 8번 - 보안 용어

소프트웨어가 불법으로 변경되었을 경우 정상적으로 수행되지 않게 하는 보안 기법의 이름을 쓰시오.

---

## 9번 - C언어 스택

스택을 구현한 다음 코드의 실행 결과를 쓰시오.

(스택 구현 코드 - 실행 결과를 작성)

---

## 10번 - 데이터베이스 설계 순서

다음 보기를 데이터베이스 설계 순서대로 나열하시오.

> 보기: 구현, 요구조건 분석, 개념적 설계, 물리적 설계, 논리적 설계

---

## 11번 - 디자인 패턴

다음 설명에 해당하는 디자인 패턴의 이름을 쓰시오.

1. 생성자를 여러 번 호출하더라도 실제로 생성되는 객체는 하나인 패턴
2. 호스트 객체의 내부 상태에 접근하여 연산을 추가하는 패턴

---

## 12번 - 오류검출/검정

다음 설명에 해당하는 용어를 쓰시오.

1. 오류를 검출하고 수정까지 가능한 코드
2. 전진 오류 수정 방식
3. 후진 오류 수정 방식
4. 패리티 비트를 이용한 검사 방식
5. 다항식을 기반으로 한 검사 방식

---

## 13번 - HDLC 프레임

다음 설명에 해당하는 HDLC 관련 용어를 쓰시오.

1. 정보 프레임
2. 감독 프레임
3. 비번호 프레임
4. 비동기 균형 모드
5. 비동기 응답 모드

---

## 14번 - Java 문자열 비교

다음 코드의 출력 결과를 쓰시오.

```java
String str1 = "Programming";
String str2 = "Programming";
String str3 = new String("Programming");
System.out.println(str1==str2);
System.out.println(str1==str3);
System.out.println(str1.equals(str3));
System.out.print(str2.equals(str3));
```

---

## 15번 - 암호화 알고리즘 분류

다음 암호화 알고리즘을 대칭키와 비대칭키로 분류하시오.

> 보기: DES, AES, ARIA, SEED, RSA, ECC

---

## 16번 - 해시

데이터를 고정된 길이의 값으로 변환하며, 검색과 암호화에 사용되는 자료구조/기법의 이름을 쓰시오.

---

## 17번 - SQL DROP VIEW

다음 SQL문의 빈칸에 들어갈 옵션을 쓰시오. (참조하는 모든 객체도 함께 삭제)

```sql
DROP VIEW 학생 (         )
```

---

## 18번 - 선택정렬 연산자

오름차순 선택정렬을 구현할 때, if문에서 사용해야 하는 비교 연산자를 쓰시오.

---

## 19번 - Python 문자열 슬라이싱

다음 코드의 출력 결과를 쓰시오.

```python
a = "engineer information processing"
b = a[:3]
c = a[4:6]
d = a[28:]
e = b+c+d
print(e)
```

---

## 20번 - 테스트 모듈

다음 설명에 해당하는 테스트 모듈의 이름을 쓰시오.

1. 상위 모듈은 존재하지만 하위 모듈이 없을 때 사용하는 테스트 모듈
2. 하위 모듈은 존재하지만 상위 모듈이 없을 때 사용하는 테스트 모듈
