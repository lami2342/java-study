# 2025년 1회 정보처리기사 실기 복원문제

---

## 1번 - 네트워크 보안

괄호 안에 알맞은 용어를 작성하시오.

- '세션을 가로채다'는 의미로 다른 사람의 세션 상태를 훔치거나 도용하여 액세스하는 해킹 기법
- TCP의 3-way 핸드셰이크 완료 후 공격자가 시퀀스 번호를 조작하여 정상적인 세션을 가로채는 공격

---

## 2번 - 데이터베이스 제약조건

보기에서 알맞은 것을 선택하시오. (보기: 개체, 참조, 도메인)

- ㄱ. ( )
- ㄴ. ( )
- ㄷ. ( )

---

## 3번 - 오류 검출 기법

오류를 감지하는 오류 검출 코드의 영문 약자(3글자)를 작성하시오.

- 데이터에 체크섬을 추가하는 기법
- 다항식(x³ + x + 1)을 기반으로 2진수 나눗셈하는 방식

---

## 4번 - 악성코드

보기에서 알맞은 것을 선택하시오.

- 사회 공학을 사용하여 충격, 불안, 위협에 대한 인식을 유발하는 악성 소프트웨어
- '겁을 주다'에서 유래한 공포를 이용한 악성코드
- 가짜 바이러스 경고나 시스템 문제 표시로 돈을 지불하도록 속이는 방식

---

## 5번 - Java 예외 처리

다음 코드의 실행 결과를 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        int a = 5, b = 0;
        try {
            System.out.print(a / b);
        } catch (ArithmeticException e) {
            System.out.print("출력1");
        } catch (ArrayIndexOutOfBoundsException e) {
            System.out.print("출력2");
        } catch (NumberFormatException e) {
            System.out.print("출력3");
        } catch (Exception e) {
            System.out.print("출력4");
        } finally {
            System.out.print("출력5");
        }
    }
}
```

---

## 6번 - 네트워크 프로토콜

괄호 안에 알맞은 프로토콜명을 작성하시오.

1. ( ) : IP 주소를 MAC 주소로 변환하는 프로토콜
2. ( ) : MAC 주소를 IP 주소로 변환하는 프로토콜

---

## 7번 - SQL 쿼리

다음 SQL 쿼리의 실행 결과를 작성하시오.

```sql
SELECT name, incentive
FROM emp, sal
WHERE emp.id = sal.id AND incentives >= 500
```

---

## 8번 - 데이터베이스 용어

보기에서 알맞은 것을 선택하시오.

(보기: ㄱ. domain, ㄴ. primary, ㄷ. degree, ㄹ. candidate, ㅁ. cardinality, ㅂ. attribute, ㅅ. foreign)

1. 릴레이션에서 속성의 개수: ( )
2. 릴레이션에서 튜플의 개수: ( )
3. 다른 릴레이션의 기본 키를 참조하는 속성: ( )
4. 특정 속성에 입력될 수 있는 값의 유형이나 범위: ( )

---

## 9번 - 서브넷 계산

IP 주소 192.168.35.10, 서브넷 마스크 255.255.252.0일 때, 브로드캐스팅 수신이 가능한 IP를 모두 고르시오.

- ㄱ. 192.168.34.1
- ㄴ. 192.168.32.19
- ㄷ. 192.168.35.200
- ㄹ. 192.168.33.138
- ㅁ. 192.168.35.50

---

## 10번 - C언어 배열 조작

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
char Data[5] = {'B', 'A', 'D', 'E'};
char c;
int main() {
    int i, temp, temp2;
    c = 'C';
    printf("%d\n", Data[3] - Data[1]);
    for (i = 0; i < 5; ++i) {
        if (Data[i] > c)
            break;
    }
    temp = Data[i];
    Data[i] = c;
    i++;
    for (; i < 5; ++i) {
        temp2 = Data[i];
        Data[i] = temp;
        temp = temp2;
    }
    for (i = 0; i < 5; i++) {
        printf("%c", Data[i]);
    }
}
```

---

## 11번 - C언어 2D 배열 동적 할당

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
#include <stdlib.h>
void set(int** arr, int* data, int rows, int cols) {
    for (int i = 0; i < rows * cols; ++i) {
        arr[((i + 1) / rows) % rows][(i + 1) % cols] = data[i];
    }
}
int main() {
    int rows = 3, cols = 3, sum = 0;
    int data[] = {5, 2, 7, 4, 1, 8, 3, 6, 9};
    int** arr;
    arr = (int**)malloc(sizeof(int*) * rows);
    for (int i = 0; i < cols; i++) {
        arr[i] = (int*)malloc(sizeof(int) * cols);
    }
    set(arr, data, rows, cols);
    for (int i = 0; i < rows * cols; i++) {
        sum += arr[i / rows][i % cols] * (i % 2 == 0 ? 1 : -1);
    }
    for (int i = 0; i < rows; i++) {
        free(arr[i]);
    }
    free(arr);
    printf("%d", sum);
}
```

---

## 12번 - 결합도

보기에서 알맞은 결합도 유형을 선택하시오.

(보기: ㄱ. 자료, ㄴ. 스탬프, ㄷ. 제어, ㄹ. 공통, ㅁ. 내용, ㅂ. 외부)

1. 다른 모듈 내부의 변수나 기능을 사용하는 결합도: ( )
2. 배열, 오브젝트, 자료구조 등이 전달되는 결합도: ( )
3. 전역 변수를 참조하고 갱신하는 결합도: ( )

---

## 13번 - Java 상속 및 오버라이딩

다음 코드의 실행 결과를 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        new Child();
        System.out.println(Parent.total);
    }
}
class Parent {
    static int total = 0;
    int v = 1;
    public Parent() {
        total += (++v);
        show();
    }
    public void show() {
        total += total;
    }
}
class Child extends Parent {
    int v = 10;
    public Child() {
        v += 2;
        total += v++;
        show();
    }
    @Override
    public void show() {
        total += total * 2;
    }
}
```

---

## 14번 - 디자인 패턴

다음이 설명하는 디자인 패턴의 이름을 작성하시오.

- 서로 다른 인터페이스를 가진 클래스들을 연결
- 기존 클래스를 원하는 인터페이스에 맞게 변환
- 기존 클래스를 감싸서 인터페이스 변환

---

## 15번 - 제어 흐름도 및 문장 커버리지

다음 C 코드의 제어 흐름도를 작성하고, 문장 커버리지를 만족하는 경로를 작성하시오.

```c
int Main(int b[], int m, int x) {
    int a = 0;
    while (a < m || b[a] < x) {
        if (b[a] < 0)
            b[a] = -b[a];
        a++;
    }
    return 1;
}
```

---

## 16번 - Java 재귀함수

다음 코드의 실행 결과를 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        int[] data = {3, 5, 8, 12, 17};
        System.out.println(func(data, 0, data.length - 1));
    }
    static int func(int[] a, int st, int end) {
        if (st >= end) return 0;
        int mid = (st + end) / 2;
        return a[mid] + Math.max(func(a, st, mid), func(a, mid + 1, end));
    }
}
```

---

## 17번 - Python 트리 순회

다음 코드의 실행 결과를 작성하시오.

```python
class Node:
    def __init__(self, value):
        self.value = value
        self.children = []

def tree(li):
    nodes = [Node(i) for i in li]
    for i in range(1, len(li)):
        nodes[(i - 1) // 2].children.append(nodes[i])
    return nodes[0]

def calc(node, level=0):
    if node is None:
        return 0
    return (node.value if level % 2 == 1 else 0) + sum(calc(n, level + 1) for n in node.children)

li = [3, 5, 8, 12, 15, 18, 21]
root = tree(li)
print(calc(root))
```

---

## 18번 - C언어 연결 리스트

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
#include <stdlib.h>
typedef struct Data {
    int value;
    struct Data *next;
} Data;

Data* insert(Data* head, int value) {
    Data* new_node = (Data*)malloc(sizeof(Data));
    new_node->value = value;
    new_node->next = head;
    return new_node;
}

Data* reconnect(Data* head, int value) {
    if (head == NULL || head->value == value) return head;
    Data *prev = NULL, *curr = head;
    while (curr != NULL && curr->value != value) {
        prev = curr;
        curr = curr->next;
    }
    if (curr != NULL && prev != NULL) {
        prev->next = curr->next;
        curr->next = head;
        head = curr;
    }
    return head;
}

int main() {
    Data *head = NULL, *curr;
    for (int i = 1; i <= 5; i++)
        head = insert(head, i);
    head = reconnect(head, 3);
    for (curr = head; curr != NULL; curr = curr->next)
        printf("%d", curr->value);
    return 0;
}
```

---

## 19번 - C언어 비트 연산 및 구조체

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
typedef struct student {
    char* name;
    int score[3];
} Student;

int dec(int enc) {
    return enc & 0xA5;
}

int sum(Student* p) {
    return dec(p->score[0]) + dec(p->score[1]) + dec(p->score[2]);
}

int main() {
    Student s[2] = {
        {"Kim", {0xA0, 0xA5, 0xDB}},
        {"Lee", {0xA0, 0xED, 0x81}}
    };
    Student* p = s;
    int result = 0;
    for (int i = 0; i < 2; i++) {
        result += sum(&s[i]);
    }
    printf("%d", result);
    return 0;
}
```

---

## 20번 - Java 메서드 오버로딩 및 재귀

다음 코드의 실행 결과를 작성하시오.

```java
public class Main {
    public static void main(String[] args) {
        System.out.println(calc("5"));
    }
    static int calc(int value) {
        if (value <= 1) return value;
        return calc(value - 1) + calc(value - 2);
    }
    static int calc(String str) {
        int value = Integer.valueOf(str);
        if (value <= 1) return value;
        return calc(value - 1) + calc(value - 3);
    }
}
```
