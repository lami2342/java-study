# 2025년 3회 정보처리기사 실기 복원문제

---

## 1번 - UML 다이어그램

괄호 안에 알맞은 용어를 작성하시오.

- UML ( ) 다이어그램은 폴더 모양의 ( ) 단위로 구성 요소 간의 관계를 표현하는 구조 다이어그램이다.

---

## 2번 - 소프트웨어 테스트 커버리지

보기에서 알맞은 것을 선택하시오.

(보기: ㄱ. 경로 ㄴ. 결정 ㄷ. 조건/결정 ㄹ. 변경 조건/결정 ㅁ. 다중 조건 ㅂ. 문장 ㅅ. 분기 ㅇ. 조건 ㅈ. 루프)

- 각 조건식이 True와 False 두 가지 경우를 모두 한 번 이상 만족하도록 테스트 케이스를 설계해야 하는 커버리지 기준은? ( )

---

## 3번 - 유닉스/리눅스 명령어

다음 설명에 해당하는 유닉스/리눅스 명령어를 각각 작성하시오.

1. 현재 작업 중인 디렉터리의 경로를 출력하는 명령어
2. 디렉터리의 내용을 목록으로 표시하는 명령어
3. 다른 디렉터리로 이동하는 명령어
4. 파일을 복사하는 명령어

---

## 4번 - 오류 검출 및 수정

괄호 안에 알맞은 용어를 작성하시오.

1. ( ) 코드 - 오류를 검출하고 수정까지 가능한 코드
2. ( ) 방식 - 전진 오류 수정 방식
3. ( ) 방식 - 후진 오류 수정 방식
4. ( ) 검사 - 패리티 비트를 이용한 오류 검출 방식
5. ( ) 검사 - 다항식을 기반으로 한 오류 검출 방식

---

## 5번 - C언어 구조체 포인터

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
struct Test {
    int i;
    const char *g;
};
int main() {
    struct Test test[] = {{1, "AB"}, {2, "DC"}, {3, "EB"}};
    struct Test *p = &test[1];
    printf("%s", p->g + (p->i - 1));
    return 0;
}
```

---

## 6번 - C언어 문자열 처리

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
int main(void) {
    char str[] = "REPUBLICOFKOREA";
    int a = 0;
    while (str[a] != '\0')
        ++a;
    putchar(str[a - 2]);
    return 0;
}
```

---

## 7번 - C언어 연결 리스트와 비트 연산

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
struct Node {
    struct Node* next;
    unsigned int x;
};
int main() {
    struct Node t1 = { 0, 5u };
    struct Node t2 = { 0, 7u };
    struct Node t3 = { 0, 11u };
    t3.next = &t2;
    t2.next = &t1;
    struct Node* curr = &t3;
    int sum = 0;
    while (curr) {
        sum = sum * 3 + curr->x;
        curr = curr->next;
    }
    sum = (sum ^ 42u) + 100u;
    printf("%u\n", sum);
}
```

---

## 8번 - Java 인터페이스

다음 Java 코드의 빈칸에 알맞은 키워드를 작성하시오.

```java
class WashingMachine (빈칸) Machine {
    ...
}
```

- Machine은 인터페이스이며, WashingMachine 클래스가 이를 구현하고 있다.

---

## 9번 - Python 리스트 처리

다음 코드의 실행 결과를 작성하시오.

```python
data = [
    [3, 5, 2, 4, 1],
    [4, 5, 1],
    [4, 4, 1, 5, 4],
    [4, 5]
]
result = {}
for index, lis in enumerate(data):
    list_sum = sum(lis)
    list_len = len(lis)
    result[index] = (list_sum, list_len)
print(result)
```

---

## 10번 - SQL 조건 실행 결과

테이블 조건값에 따른 실행 결과를 작성하시오.

---

## 11번 - 인증 기술

괄호 안에 알맞은 용어를 작성하시오.

- 한 번 사용하면 즉시 폐기되어 재사용이 불가능한 인증 방식
- 시간 동기화 또는 카운터 기반으로 동작
- 해시 함수를 이용하여 일회용 비밀번호를 생성하는 인증 기술

---

## 12번 - Java 상속과 생성자

다음 Java 코드의 빈칸에 알맞은 키워드를 작성하시오.

```java
class Rectangle {
    int width, height;

    Rectangle(int width, int height) {
        this.width = width;
        this.height = height;
    }
}

class Square extends Rectangle {
    Square(int a) {
        ____(a, a);
    }
}
```

---

## 13번 - 인증/인가 프로토콜

괄호 안에 알맞은 용어를 작성하시오.

- 사용자가 새로운 사이트에 별도 가입 없이 기존에 사용하던 서비스의 계정으로 로그인할 수 있는 기술
- 비밀번호를 직접 전달하지 않고 인가(Authorization) 절차를 수행
- Access Token을 발급하여 리소스 접근을 제어

---

## 14번 - 관계대수 나누기 연산

테이블 R과 S가 주어졌을 때, R % S (나누기 연산)의 결과를 테이블 형태로 작성하시오.

---

## 15번 - C언어 조건 연산자와 비트 연산

다음 코드의 실행 결과를 작성하시오.

```c
#include <stdio.h>
int main() {
    int x=7, y=4, z;
    z = y%3<3 ? 2 : 1;
    z = z & z >> 1;
    z = x>5 && z<=3 ? z*x : z/x;
    printf("%d", z);
    return 0;
}
```

---

## 16번 - 관계형 데이터베이스 용어

다음 설명에 해당하는 관계형 데이터베이스 용어를 각각 작성하시오.

- ㄱ. 릴레이션에서 하나의 행(Row)을 의미하는 용어
- ㄴ. 특정 시점에서 릴레이션에 저장된 데이터의 상태 전체를 의미하는 용어
- ㄷ. 릴레이션에서 행(Row)의 총 개수를 의미하는 용어

---

## 17번 - Java enum

다음 코드의 실행 결과를 작성하시오.

```java
enum Tri {
    A("A"), B("AB"), C("ABC");
    private String code;
    Tri(String code) {
        this.code = code;
    }
    public String code() {
        return code;
    }
}
public class Main {
    public static void main(String[] args) {
        Tri t = Tri.values()[Tri.A.name().length()];
        System.out.print(t.code());
    }
}
```

---

## 18번 - 접근통제 모델

다음 설명에 해당하는 접근통제(Access Control) 모델을 각각 작성하시오.

- ㄱ. 중앙에서 보안 정책을 일괄적으로 설정하여 관리하는 방식
- ㄴ. 사용자의 직무나 역할에 따라 권한을 부여하는 방식
- ㄷ. 자원의 소유자가 자유롭게 권한을 부여하는 방식

---

## 19번 - 테스트케이스 구성요소

테스트케이스 구성요소의 빈칸에 알맞은 용어를 작성하시오.

- ㄱ. ( ) - 테스트를 수행하기 위한 전제 조건
- ㄹ. ( ) - 테스트 실행에 필요한 입력값
- ㅁ. ( ) - 테스트 수행 후 기대되는 결과

---

## 20번 - SQL 쿼리

다음 SQL 쿼리의 실행 결과를 작성하시오.

```sql
SELECT count(col2) FROM A WHERE col1 IN (2, 3) OR col2 IN (3, 5)
```
