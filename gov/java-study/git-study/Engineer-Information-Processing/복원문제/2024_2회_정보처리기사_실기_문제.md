# 2024년 2회 정보처리기사 실기 복원문제

---

## 1번. Java 코드 출력값

다음 Java 코드의 출력값을 작성하시오.

```java
class Main {
    public static void main(String[] args) {
        int[] a = new int[]{1, 2, 3, 4};
        int[] b = new int[]{1, 2, 3, 4};
        int[] c = new int[]{1, 2, 3};
        check(a, b);
        check(a, c);
        check(b, c);
    }
    public static void check(int[] a, int[] b) {
        if (a==b) {
            System.out.print("O");
        }else{
            System.out.print("N");
        }
    }
}
```

---

## 2번. 데이터베이스 용어

다음이 설명하는 데이터베이스 용어를 작성하시오.

> 데이터를 중복시켜 성능을 향상시키는 기법으로, 데이터를 중복 저장하거나 테이블을 합치는 방법을 사용한다. 무결성이 저하될 수 있다.

---

## 3번. SQL 구문 빈칸 채우기

다음 SQL 구문의 빈칸 ①~④에 들어갈 키워드를 작성하시오.

```sql
INSERT INTO 사원 (사원번호, 이름, 주소, 부서) [①] (32431, '정실기', '서울', '영업');

INSERT INTO 부서 (사원번호, 이름, 나이, 부서)
[②] 사원번호, 이름, 나이, 23 FROM 사원 WHERE 이름 = '정실기';

SELECT * [③] 사원;

UPDATE 사원 [④] 부서 = '퇴사' WHERE 사원번호 = 32431;
```

---

## 4번. 릴레이션의 Cardinality와 Degree

주어진 릴레이션에서 Cardinality와 Degree를 각각 구하시오.

---

## 5번. 네트워크 보안

다음이 설명하는 네트워크 보안 프로토콜의 이름을 작성하시오.

> Network Layer에서 IP 패킷을 암호화하고 인증하는 프로토콜이다. AH(Authentication Header)와 ESP(Encapsulating Security Payload) 프로토콜을 사용한다.

---

## 6번. Python 코드 출력값

다음 Python 코드의 출력값을 작성하시오.

```python
def fnCalculation(x, y):
    result = 0;
    for i in range(len(x)):
        temp = x[i:i+len(y)]
        if temp == y:
            result += 1;
    return result

a = "abdcabcabca"
p1 = "ab";
p2 = "ca";
out = f"ab{fnCalculation(a,p1)}ca{fnCalculation(a,p2)}"
print(out)
```

---

## 7번. 암호화 알고리즘

다음이 설명하는 암호화 알고리즘의 이름을 작성하시오.

> 1997년 NIST에서 DES를 대체하기 위해 공모하여 선정된 대칭키 암호 알고리즘이다. 128/192/256비트의 가변 키 길이를 지원하며, 블록 크기는 128비트로 고정이다.

---

## 8번. 패킷 교환 방식

다음이 설명하는 패킷 교환 방식의 이름을 각각 작성하시오.

> ① 논리적 연결을 설정하여 통신하는 방식
> ② 각 패킷이 독립적으로 경로를 설정하여 전달되는 방식

---

## 9번. 응집도

다음이 설명하는 응집도의 유형을 작성하시오.

> 모듈 내의 기능들이 실행 순서가 밀접하게 관련되어 있으며, 한 기능의 출력 데이터가 다음 기능의 입력 데이터로 사용된다.

---

## 10번. 디자인 패턴

다음이 설명하는 디자인 패턴의 이름을 작성하시오.

> 컬렉션의 내부 구조를 노출하지 않으면서 원소들에 순차적으로 접근할 수 있도록 하는 패턴이다.

---

## 11번. 라우팅 (RIP 최단 경로)

RIP 라우팅 프로토콜을 사용할 때, A에서 F까지의 최단 경로를 구하시오.

---

## 12번. SRT 스케줄링 평균 대기시간

SRT(Shortest Remaining Time) 스케줄링을 적용할 때, 평균 대기시간을 구하시오.

| 프로세스 | 도착 시간 | 서비스 시간 |
|---------|----------|------------|
| A       | 0        | 8          |
| B       | 1        | 4          |
| C       | 2        | 9          |
| D       | 3        | 5          |

---

## 13번. C언어 코드 출력값

다음 C언어 코드의 출력값을 작성하시오.

```c
#include <stdio.h>
int main() {
    int arr[3][3] = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    int* parr[2] = {arr[1], arr[2]};
    printf("%d", parr[1][1] + *(parr[1]+2) + **parr);
    return 0;
}
```

---

## 14번. Java 코드 출력값 (홀수/짝수 합)

주어진 Java 코드에서 홀수의 합과 짝수의 합을 각각 구하시오.

---

## 15번. C언어 코드 출력값 (문자열 복사 및 인덱스 합)

주어진 C언어 코드의 출력값을 작성하시오. (문자열 복사 후 인덱스 합을 구하는 문제)

---

## 16번. 결합도

다음이 설명하는 결합도의 유형을 작성하시오.

> 한 모듈이 다른 모듈의 논리적 흐름을 제어하기 위해 제어 요소(플래그, 스위치 등)를 전달하는 결합도이다.

---

## 17번. Java 재귀함수 (중복 제거)

주어진 Java 재귀함수 코드의 출력값을 작성하시오. (문자열에서 중복을 제거하는 문제)

---

## 18번. C언어 switch문 코드

주어진 C언어 switch문 코드의 출력값을 작성하시오.

---

## 19번. C언어 연결리스트 구조체

주어진 C언어 연결리스트(구조체) 코드의 출력값을 작성하시오.

---

## 20번. Java String split()

다음 Java 코드의 출력값을 작성하시오.

```java
String str = "ITISTESTSTRING";
String[] result = str.split("T");
System.out.print(result[3]);
```

---
